<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>nl.vpro.media</groupId>
  <artifactId>media-gui</artifactId>
  <packaging>war</packaging>
  <version>5.11-SNAPSHOT</version>
  <name>media-gui</name>

  <scm>
    <connection>scm:svn:https://subversion.vpro.nl/repo/nl/vpro/media-gui/trunk</connection>
    <developerConnection>scm:svn:https://subversion.vpro.nl/repo/nl/vpro/media-gui/trunk</developerConnection>
    <url>https://subversion.vpro.nl/viewvc/nl/vpro/media-gui/trunk</url>
  </scm>
  <profiles>
    <profile>
      <id>npm</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>com.github.eirslett</groupId>
            <artifactId>frontend-maven-plugin</artifactId>
            <version>1.6</version>
            <executions>
              <execution>
                <id>install node and npm</id>
                <goals>
                  <goal>install-node-and-npm</goal>
                </goals>
                <configuration>
                  <nodeVersion>v6.14.4</nodeVersion>
                  <npmVersion>3.10.10</npmVersion>
                </configuration>
              </execution>
              <execution>
                <id>npm install</id>
                <goals>
                  <goal>npm</goal>
                </goals>
                <configuration>
                  <arguments>install</arguments>
                </configuration>
              </execution>
              <execution>
                <id>npm build</id>
                <goals>
                  <goal>npm</goal>
                </goals>
                <configuration>
                  <arguments>run deploy:gulp</arguments>
                </configuration>
              </execution>
              <!-- It's broken and it has never found any bugs any ways
              I thing the maintainance is not worth the trouble.
              Selenium tests may be more usefull?
              https://github.com/npo-poms/functional-tests/tree/master/poms-functional-tests-selenium
              <execution>
                <id>karma tests</id>
                <goals>
                  <goal>karma</goal>
                </goals>
                <configuration>
                  <karmaConfPath>karma.ci.conf.js</karmaConfPath>
                </configuration>
              </execution>
              -->
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
  <build>
    <defaultGoal>install</defaultGoal>
    <plugins>
      <plugin>
        <artifactId>maven-war-plugin</artifactId>
        <version>3.2.2</version>
        <configuration>
          <warSourceDirectory>build/deploy</warSourceDirectory>
          <failOnMissingWebXml>false</failOnMissingWebXml>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-clean-plugin</artifactId>
        <version>3.1.0</version>
        <configuration>
          <!-- the frontend plugin sucks horribly and installs all over the place in src dir.-->
          <filesets>
            <fileset>
              <directory>${basedir}</directory>
              <includes>
                <include>src/js/poms/constants.js</include>
                <include>build/**</include>
                <include>node_modules/**</include>
                <include>node/**</include>
              </includes>
            </fileset>
          </filesets>
        </configuration>
      </plugin>
    </plugins>
  </build>



  <distributionManagement>
    <repository>
      <id>nexus</id>
      <name>Nexus releases</name>
      <url>http://nexus.vpro.nl/content/repositories/releases</url>
    </repository>
    <snapshotRepository>
      <id>nexus</id>
      <name>Nexus snapshots</name>
      <url>http://nexus.vpro.nl/content/repositories/snapshots</url>
    </snapshotRepository>
  </distributionManagement>

</project>
